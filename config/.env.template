# Voice-OpenClaw Bridge v2 Environment Variables
# Copy this file to ~/.voice-bridge/.env and fill in your values

# OpenClaw API Key (if required)
# OPENCLAW_API_KEY=your_api_key_here

# Custom OpenClaw host (overrides config file)
# OPENCLAW_HOST=localhost
# OPENCLAW_PORT=8080

# Whisper STT model path (if using custom model)
# WHISPER_MODEL_PATH=/path/to/custom/model

# Piper TTS model path (if using custom voice)
# PIPER_MODEL_PATH=/path/to/custom/voice.onnx

# Debug mode (overrides config file)
# BRIDGE_LOG_LEVEL=DEBUG
